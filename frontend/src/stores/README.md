# Stores Directory

Zustand stores for client-side state management.

## Planned Stores

- `ui-store.ts` - UI state (sidebar open/closed, modals, etc.)
- `session-store.ts` - Interview session state
- `recording-store.ts` - Recording state
